<%= render "fonts" %>

<% if @presenter.reading_mode? %>
  <% pages = @presenter.paginate.group_by(&:page_number) %>
  <%= render partial: 'page', collection: pages, as: :verses %>
<% else %>
  <%= render partial: 'verse', collection: @presenter.paginate, as: :verse %>
<% end %>
